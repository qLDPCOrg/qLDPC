# Documentation build dependencies (used by Read the Docs).
#
# These are the Sphinx toolchain packages needed to build the docs.  They are
# NOT part of qLDPC's runtime or "dev" dependencies, so they must be listed
# explicitly here.  The qLDPC package itself is installed separately via the
# `python.install` section of ../.readthedocs.yaml.
sphinx>=7
sphinx-autoapi>=3.0
nbsphinx>=0.9
sphinx-rtd-theme>=2.0
ipython>=8.0
